/**
 * Pharmacy microservice runs on its own port and is not yet routed
 * through the API gateway. Update this URL here once gateway routing
 * for /medicines and /dispense is configured.
 */
// export const PHARMACY_API_URL = 'http://localhost:8084';
